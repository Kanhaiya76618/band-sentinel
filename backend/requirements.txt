# Offline incident demo (zero keys) needs only:
pydantic>=2

# Platform (web dashboard + jobs workflow): python -m frontend.server
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27            # live LLMs, Resend, Adzuna, SSE driving

# Resume parsing (Phase 3) + downloadable reports (Phase 4)
pdfplumber>=0.11       # PDF resumes
python-docx>=1.1       # DOCX resumes + tailored-resume output
reportlab>=4.0         # PDF report / resume rendering
band-sdk
